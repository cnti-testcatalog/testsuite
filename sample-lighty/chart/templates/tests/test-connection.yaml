apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-lighty.fullname" . }}-test-connection"
  labels:
    {{- include "sample-lighty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-lighty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
