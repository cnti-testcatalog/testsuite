apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-lighty.fullname" . }}
  labels:
    {{- include "sample-lighty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-lighty.selectorLabels" . | nindent 4 }}
