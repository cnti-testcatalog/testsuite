---
config_version: v2
deployments:
  helm_dirs:
  # (kosstennbl) TODO: move helm addoional arguments from name to helm_values
  - name: test --set mariadb.primary.persistence.enabled=false --set persistence.enabled=false
    helm_directory: wordpress
