---
config_version: v2
deployments:
  helm_dirs:
  - name: test
    helm_directory: jaeger
    namespace: jaeger
  # Repository was there, but it seems unnecessary:
  # helm_repo_name: jaegertracing 
  # helm_repo_url: https://jaegertracing.github.io/helm-charts